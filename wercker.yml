box: mitsuse/box-activator
build:
    steps:
        - script:
            name: build
            code: |
                activator compile
        - script:
            name: test
            code: |
                activator test
